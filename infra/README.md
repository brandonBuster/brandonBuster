# Infrastructure (Terraform)

Terraform lives here. **Phase 1** will add:

- `envs/dev`, `envs/prod`
- `modules/` (e.g. frontdoor_waf, static_site, apim, container_app_api, key_vault, observability, dns)

See **requirements.md** §5 and **roadmap.md** Phase 1. Step 0 only requires this folder to exist.
